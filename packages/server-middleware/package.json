{
  "name": "@fiber-paid-http/server-middleware",
  "version": "0.1.0",
  "type": "module",
  "description": "Fetch/Hono-compatible Fiber Paid HTTP paid API middleware.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fiber-paid-http/core": "workspace:*",
    "@fiber-paid-http/fiber-method": "workspace:*",
    "@fiber-paid-http/fl402-compat": "workspace:*",
    "@fiber-paid-http/storage": "workspace:*"
  }
}
