{
  "name": "@fiber-paid-http/core",
  "version": "0.1.0",
  "type": "module",
  "description": "MPP Payment authentication types, Fiber charge method schemas, JCS encoding, and HTTP helpers.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "json-canonicalize": "^2.0.0",
    "zod": "^3.25.0"
  }
}
