{
  "name": "@fiber-paid-http/core",
  "version": "0.1.0",
  "type": "module",
  "description": "Core Fiber Paid HTTP protocol types, signing, serialization, and HTTP helpers.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "zod": "^3.25.0"
  }
}
